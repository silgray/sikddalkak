import { it } from 'vitest';

// 실제 내용은 전부 주석 처리된 실험 파일 — vitest가 "No test suite found"로
// 실패 처리하는 걸 막는 자리 표시자 (CI 배포 게이트 통과용).
it.skip('placeholder', () => {});

// describe('ce_parse 위임 테스트', () => {
//   it('pure literals are pre-calculated', () => {
//     expect()
//   });
// });